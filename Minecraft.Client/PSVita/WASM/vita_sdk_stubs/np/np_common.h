#pragma once
/*
 * Minimal PS Vita NP common SDK stubs for Emscripten/WASM builds.
 * np/np_common.h is included by 4J_Profile.h and provides basic NP types.
 * Those types are all defined in the parent np.h stub, so just include it.
 */

#include "../np.h"
